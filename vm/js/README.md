# μFork for JavaScript

A JavaScript host built around the [uFork WASM component](../wasm/README.md).

It is compatible with the browser, [Deno](https://deno.com), and
[Node.js](https://nodejs.org/).
